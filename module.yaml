src:
  build:
    target: None
    configuration: None

notests *default:
  build:
    target: Vostok.Commons.Collections.sln
    configuration: NoTests
    tool:
      name: dotnet

  deps:
    - vostok.devtools

full-build > notests:
  build:
    target: Vostok.Commons.Collections.sln
    configuration: Release
